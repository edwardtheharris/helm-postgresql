---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: "{{ .Release.Name }}"
spec:
  gatewayClassName: eg
  listeners:
  - name: {{ .Release.Name }}
    protocol: TCP
    port: 5432
    allowedRoutes:
      kinds:
      - kind: TCPRoute

